name: Sync from Google Drive

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Google API Client
        run: |
          pip install google-api-python-client google-auth google-auth-httplib2 google-auth-oauthlib

      - name: Save service account key to file
        env:
          GDRIVE_SERVICE_KEY: ${{ secrets.GDRIVE_SERVICE_KEY }}
        run: echo "$GDRIVE_SERVICE_KEY" > service_account.json

      - name: Run Google Drive Download Script
        run: python3 .github/scripts/gdrive_download.py

      - name: Unzip ZIP File
        run: |
          rm -rf project && mkdir project
          unzip -o "Crew Management System (CMS).zip" -d project
          cp -r project/* ./

      - name: Commit and Push Changes
        run: |
          git config user.name "drive-sync-bot"
          git config user.email "bot@example.com"
          git add .
          git commit -m "Auto-sync from Google Drive"
          git push || echo "No changes to commit"
